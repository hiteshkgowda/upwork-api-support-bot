# --- RAG framework (Assignment "Required Stack": LangChain) ---------------
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0           # OpenAI-compatible LLM client (HF/Ollama/vLLM)
langchain-huggingface>=0.1.0      # local embeddings wrapper
langchain-text-splitters>=0.3.0   # RecursiveCharacterTextSplitter

# --- Local embedding model (Assignment A3) --------------------------------
sentence-transformers>=3.0.0      # all-MiniLM-L6-v2 runs locally

# --- Vector database (Assignment "Required Stack": FAISS) ------------------
faiss-cpu>=1.8.0

# --- Document loading -----------------------------------------------------
pypdf>=5.0.0                       # read the Upwork API reference PDF

# --- UI (Assignment B3) ---------------------------------------------------
streamlit>=1.38.0

# --- Config / secrets -----------------------------------------------------
python-dotenv>=1.0.0              # load .env (keeps API keys out of source)
